# 使用 Node.js 20 Alpine 镜像
FROM node:20-alpine

# 设置工作目录
WORKDIR /app

# 复制 package.json
COPY package.json ./

# 安装依赖 (使用 npm)
RUN npm install

# 复制所有文件
COPY . .

# 暴露端口
EXPOSE 8003

# 启动开发服务器
CMD ["npm", "run", "docs:dev", "--", "--host", "0.0.0.0", "--port", "8003"]
