# 飞书机器人 (Feishu) 接入指南

飞书机器人支持通过 WebSocket 长连接与 CatWiki 通信，无需配置外网回调 URL，配置简单且响应迅速。

## 1. 飞书开放平台配置

### 1.1 创建应用
1. 登录 [飞书开放平台](https://open.feishu.cn/app?lang=zh-CN)。
2. 点击 **“创建自定义应用”**，填写应用名称和描述，上传图标后点击创建。

### 1.2 开启机器人能力
1. 进入应用详情页，在左侧导航栏点击 **“应用能力” -> “机器人”**。
2. 点击 **“开启机器人”**。

### 1.3 配置权限范围
在左侧导航栏点击 **“开发配置” -> “权限管理”**，搜索并开启以下权限：
- **im:message.p2p_msg:readonly** (接收用户发送给机器人的单聊消息)
- **im:message.group_at_msg:readonly** (接收用户在群聊中@机器人的消息)
- **im:message:send_as_bot** (以机器人的身份发送消息)
- **im:chat:readonly** (获取群聊信息，用于群聊对话)

### 1.4 获取凭据
在左侧导航栏点击 **“开发配置” -> “凭据与基础信息”**，记录下：
- **App ID**
- **App Secret**

### 1.5 发布应用
1. 点击左侧导航栏 **“版本管理与发布” -> “创建版本”**。
2. 填写版本号（如 1.0.0）、更新说明，保存。
3. 点击 **“申请线上发布”**。如果是企业自建应用，管理员审核通过后即可生效。

---

## 2. CatWiki 后台配置

1. 登录 CatWiki 后台，进入 **“站点设置” -> “机器人配置”**。
2. 选中 **“飞书机器人”**，开启开关。
3. 填入从飞书平台获取的 `App ID` 和 `App Secret`。
4. 点击 **“保存”**。系统将自动建立 WebSocket 连接。

---

## 3. 技术特性
- **同步频率**：0.5s / 次。
- **连接方式**：WebSocket (Internal)，无需公网 IP 或域名即可使用。
