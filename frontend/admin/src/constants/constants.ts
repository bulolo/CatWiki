/**
 * 常量定义
 * 统一管理所有常量，避免在多个文件中重复定义
 */

// ==================== 站点配置常量 ====================

import { env } from "@/lib/env"

// 客户端站点 URL（用于从管理后台跳转到客户端）
export const CLIENT_SITE_URL = env.NEXT_PUBLIC_CLIENT_URL

// ==================== 模型配置常量 ====================

// ==================== 模型配置常量 ====================
// [REMOVED] Legacy provider configurations

