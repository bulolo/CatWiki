# CatWiki Nginx 配置文件
# 将此文件放置到 /etc/nginx/conf.d/ 目录下
# 或者在 /etc/nginx/nginx.conf 中 include 此文件

# admin.catwiki.cn 代理到 8001 (管理后台)
server {
    listen 80;
    server_name admin.catwiki.cn;
    
    location / {
        proxy_pass http://127.0.0.1:8001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# demo.catwiki.cn 代理到 8002 (客户端前端)
server {
    listen 80;
    server_name demo.catwiki.cn;
    
    location / {
        proxy_pass http://127.0.0.1:8002;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# catwiki.cn 代理到 8004 (官网)
server {
    listen 80;
    server_name catwiki.cn www.catwiki.cn;
    
    location / {
        proxy_pass http://127.0.0.1:8004;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# docs.catwiki.cn 代理到 8003 (文档站点)
server {
    listen 80;
    server_name docs.catwiki.cn;
    
    location / {
        proxy_pass http://127.0.0.1:8003;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# api.catwiki.cn 代理到 3000 (后端 API)
server {
    listen 80;
    server_name api.catwiki.cn;
    
    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# files.catwiki.cn 代理到 RustFS 对象存储 (9000)
server {
    listen 80;
    server_name files.catwiki.cn;
    
    # 允许较大的文件上传
    client_max_body_size 100M;
    
    location / {
        proxy_pass http://127.0.0.1:9000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # 文件服务可能需要较长超时时间
        proxy_connect_timeout 300;
        proxy_send_timeout 300;
        proxy_read_timeout 300;
    }
}
